<view template="dashboard.html">
  <label>Custom wiring the clicks</label>
  <module name="AccountBar" layoutPanel="appHeader"/>
  <module name="AppBar" layoutPanel="navigationHeader"/>
  <module name="Message" layoutPanel="messaging">
    <param name="filter">*</param>
    <param name="clearOnJobDispatch">False</param>
    <param name="maxSize">1</param>
  </module>
  <module name="TitleBar" layoutPanel="viewHeader">
    <param name="actionsMenuFilter">dashboard</param>
  </module>
 
   <module name="ServerSideInclude" layoutPanel="panel_row1_col1">
    <param name="src">
      drilldown_chart2.html
    </param>
  </module>  
  
  <!--
  <module name="StaticContentSample" layoutPanel="panel_row2_col1">
    <param name="text"><![CDATA[
      First swap out the underlying base search, that our intention gets added to.
      COMING SOON.
    ]]></param>
  </module>
  -->

  <module name="StaticContentSample" layoutPanel="panel_row2_col1">
    <param name="text"><![CDATA[
      <p><b>
        Click one of the top 10 recent sourcetypes by volume indexed.
        <br />This opens a timechart of avg(eps) for that particular sourcetype over the same period.
      </b></p>
    ]]></param>
  </module>
  <module name="HiddenSearch" layoutPanel="panel_row2_col1" autoRun="True">
    <param name="search">
      index=_internal source=*metrics.log group=per_sourcetype_thruput
      | chart sum(kb) over series | sort -sum(kb) | head 10
    </param>
    <param name="earliest">-1h</param>
    <module name="HiddenChartFormatter">
      <param name="charting.chart">bar</param>
      <param name="charting.primaryAxisTitle.text">Sourcetype</param>
      <param name="charting.secondaryAxisTitle.text">KB Indexed</param>
      <param name="charting.legend.placement">none</param>
      <module name="JobProgressIndicator"/>
      
      <!-- The JSChart containing drilldowns -->
      <module name="JSChart">
        <param name="width">100%</param>
        <param name="height">160px</param>

        <!-- Swap out the search to a timechart.  -->
        <module name="HiddenSearch">
          <param name="search">
            index=_internal source=*metrics.log group=per_sourcetype_thruput
            | timechart avg(eps)
          </param>
          <param name="earliest">-1h</param>
          <!-- 
            Grab the value clicked on and put it in a searchterm: series="someSourcetype".
          -->
          <module name="ConvertToIntention">
            <param name="intention">
              <param name="name">addterm</param>
              <param name="arg">
                <param name="series">$click.value$</param>
              </param>
              <!-- Tell the addterm intention to put the term in the first search clause no matter what. -->
              <param name="flags"><list>indexed</list></param>
            </param>

            <!--
              Finally, render the search in another JSChart,
              and throw in a JobProgressIndicator for good measure.
            -->
            <module name="JobProgressIndicator"></module>

            <!-- Throw in a header so the user sees what they clicked. -->
            <module name="SimpleResultsHeader">
              <param name="entityName">results</param>
              <param name="headerFormat">EPS over time for sourcetype=$click.value$ $time$</param>
            </module> <!-- SimpleResultsHeader -->
            <module name="HiddenChartFormatter">
              <param name="chart">line</param>
              <param name="primaryAxisTitle.text">Time</param>
              <param name="secondaryAxisTitle.text">events per second</param>
              <param name="legend.placement">none</param>
              <module name="JSChart">
                <param name="width">100%</param>
                <param name="height">160px</param>
              </module> <!-- JSChart -->
            </module> <!-- HiddenChartFormatter -->
          </module> <!-- ConvertToIntention -->
        </module> <!-- HiddenSearch -->
      </module> <!-- JSChart -->
    </module> <!-- HiddenChartFormatter -->
  </module> <!-- HiddenSearch -->


  <module name="StaticContentSample" layoutPanel="panel_row2_col2">
    <param name="text"><![CDATA[
      <p><b>
        This panel is similar to the one on the left, but uses a table visualization.
        <br />The search in the table has no 'split-by' data.
        <br />It uses row highlighting (rather than cell highlighting) to better model the click action.
      </b></p>
    ]]></param>
  </module>
  <module name="HiddenSearch" layoutPanel="panel_row2_col2" autoRun="True">
    <param name="search">
      index=_internal source=*metrics.log group=per_sourcetype_thruput
      | chart sum(kb) over series | sort -sum(kb) | head 10
    </param>
    <param name="earliest">-1h</param>
    <module name="HiddenChartFormatter">
      <param name="chart">bar</param>
      <param name="primaryAxisTitle.text">Sourcetype</param>
      <param name="secondaryAxisTitle.text">KB Indexed</param>
      <param name="legend.placement">none</param>
      <module name="JobProgressIndicator"/>
      
      <!-- The SimpleResultsTable containing drilldowns -->
      <module name="SimpleResultsTable">
        <param name="drilldown">row</param>
        <param name="entityName">results</param>

        <!-- Throw in a header to tell the user what they clicked on. -->
        <module name="SimpleResultsHeader">
          <param name="entityName">results</param>
          <param name="headerFormat">EPS over time for sourcetype=$click.value$ $time$</param>
        </module> <!-- SimpleResultsHeader -->
                
        <!-- Swap out the search to be a timechart.  -->
        <module name="HiddenSearch">
          <param name="search">
            index=_internal source=*metrics.log group=per_sourcetype_thruput
            | timechart avg(eps)
          </param>
          <param name="earliest">-1h</param>
          <!-- Grab the value clicked on and put it in as a searchterm: series="someSourcetype".   -->
          <module name="ConvertToIntention">
            <param name="intention">
              <param name="name">addterm</param>
              <param name="arg">
                <param name="series">$click.value$</param>
              </param>
              <!-- Tell the addterm intention to put the term in the first search clause no matter what. -->
              <param name="flags"><list>indexed</list></param>
            </param>

            <!--
              Finally, render the search in another JSChart,
              and throw in a JobProgressIndicator for good measure.
            -->
            <module name="JobProgressIndicator"></module>
            <module name="HiddenChartFormatter">
              <param name="chart">line</param>
              <param name="primaryAxisTitle.text">Time</param>
              <param name="secondaryAxisTitle.text">events per second</param>
              <param name="legend.placement">none</param>
              <module name="JSChart">
                <param name="width">100%</param>
                <param name="height">160px</param>
              </module> <!-- JSChart -->
            </module> <!-- HiddenChartFormatter -->
          </module> <!-- ConvertToIntention -->
        </module> <!-- HiddenSearch -->
      </module> <!-- SimpleResultsTable -->
    </module> <!-- HiddenChartFormatter -->
  </module> <!-- HiddenSearch -->
  
   <module name="ServerSideInclude" layoutPanel="panel_row3_col1">
    <param name="src">
      drilldown_chart2_layout.html
    </param>
  </module>  
  
</view>
